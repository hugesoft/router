{% extends "base.html" %}
{% import "bootstrap/wtf.html" as wtf %}

{% block title %}湖州日报手机版{% endblock %}

{% block navbar %}
<div class="btn-group btn-group-justified navbar-fixed-bottom" role="group" aria-label="...">
  <div class="btn-group dropup" role="group">
    <button type="button" class="btn btn-default dropdown-toggle" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
      往期
      <span class="caret"></span>
    </button>
    <ul class="dropdown-menu" style="height:250px;overflow:scroll">
 	{% for menu_data in menu_data %}
    <li><a href={{ menu_data.url|safe }}>{{ menu_data.name  }}</a></li>
    {% endfor %}
    </ul>
  </div>
  
  <div class="btn-group" role="group">
    <a href='/main/?url={{ curr_page }}' class="btn btn-primary active" role="button">版面</a>
  </div>
  
   <div class="btn-group" role="group">
    <a href= '/list/?url={{ curr_page }}' class="btn btn-default active" role="button">目录</a>
  </div>
</div>
{% endblock %}

{% block page_content %}
<div class="page-header">
2015年10月29日
</div>
<div class="list_group">
	{% for page_data in page_data %}
		{% if page_data.item_type == 1 %}
			<a href={{ page_data.url }} class="list-group-item active">{{ page_data.title }}</a>
		{% else %}	
			<a href={{ page_data.url}} class="list-group-item">{{ page_data.title }}</a>
		{% endif %}
	{% endfor %}
</div>
{% endblock %}
